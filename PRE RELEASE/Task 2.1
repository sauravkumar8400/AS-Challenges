name = input("What is the name of the monster you are looking for? ")

def searchm():
    Mfile = open('Monsters.txt','r')
    while True:
        line = Mfile.readline()
        if name == line:
            print("Monster Found")
        else:
            print("Monster Not Found")

